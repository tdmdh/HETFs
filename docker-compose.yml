version: '3.8'

services:
  ibgateway:
    image: ghcr.io/rsjethani/ibkr-gateway:latest
    container_name: ibkr-gateway
    ports:
      - "5000:5000"
    environment:
      # Optional: set timezone
      - TZ=UTC
    restart: unless-stopped
